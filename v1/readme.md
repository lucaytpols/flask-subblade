# flask-subblade
This example is to show you how to create a subbcount site using flask!

## Requirements:
Install the components on Linux with this cmd:

```bash
pip3 install flask
```
Install flask on Windows with this cmd:

```bash
py -m pip install flask
```
